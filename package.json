{
  "name": "affordmed-backend-eval",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dist/server.js",
    "start": "npm run build && node dist/server.js",
    "notification": "npm run build && node dist/notification_app_be/server.js",
    "schedule": "npm run build && node dist/vehicle_scheduling_be/runner.js",
    "test": "jest"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.18.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.4"
  }
}
