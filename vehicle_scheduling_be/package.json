{
  "name": "vehicle-scheduling-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --loader ts-node/esm src/runner.ts",
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/server.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.4"
  }
}
